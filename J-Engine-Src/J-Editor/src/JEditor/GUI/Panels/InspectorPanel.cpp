#include <JEditor/GUI/Panels/InspectorPanel.h>

namespace JEngine::Editor {
    InspectorPanel::InspectorPanel(const char* title, Alignment align) : IDockPanel(title, align) { }
    void InspectorPanel::draw(Application* app) {
        if (app) {

        }
    }
}